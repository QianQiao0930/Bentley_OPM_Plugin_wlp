# 管道支吊架 分发包打包说明

把「管道支吊架」及其运行依赖打成一个可换机运行的压缩包。
**源码仓库不含打包产物**（`*.zip` 已在 `.gitignore` 忽略）；本文件说明如何为
每个新版本重新生成，新增功能后照此继续打包即可。

## 1. 版本号与命名

产物命名固定为：

```text
管道支吊架_分发版本_V<主>.<次>.<修订>.zip      # 例如 管道支吊架_分发版本_V1.0.1.zip
```

- 新增支吊架类型 / 功能：次版本 +1（`V1.1.0`）
- 仅修 bug / 参数调整：修订号 +1（`V1.0.2`）
- 不兼容改动：主版本 +1（`V2.0.0`）

## 2. 包内容（顶层四目录，缺一不可）

| 目录 | 作用 | 谁在用 |
| --- | --- | --- |
| `管道支吊架/` | 主体：入口脚本 + `模块/` | —— |
| `bentley_ui/` | 共享 Tkinter 外观工具箱 | 所有入口脚本 |
| `型钢截面生成器/` | `steel_sections` 型钢截面数据 / 几何 | L 型、门型架、T 形架、E1、K1 等 |
| `管道信息查询/` | 管道 EC 属性 / 管径读取（`模块/管道信息`） | A2、E1、F10、F6_F7、K1、T4 |

### 为什么必须是「同级目录」

入口脚本按 `REPO_ROOT = dirname(管道支吊架目录)` 找依赖，即 `bentley_ui`、
`型钢截面生成器`、`管道信息查询` 都必须是 `管道支吊架` 的**同级目录**。
压缩包解压后的顶层目录（`管道支吊架_分发版本_VX.Y.Z/`）正好充当 `REPO_ROOT`，
因此换机后**无需修改任何 import 或路径**。

```text
管道支吊架_分发版本_V1.0.1/
├── bentley_ui/
├── 型钢截面生成器/
├── 管道信息查询/
└── 管道支吊架/
```

## 3. 排除规则（最小化）

- 测试：`tests/`（各目录下的测试目录）
- 缓存：`__pycache__/`、`.ruff_cache/`、`*.pyc`
- 运行产物：`模块/日志/`、`模块/输出/`、`*_debug_log.txt`、`*.log`、`*_bom.json`、`*.xlsx`
- 未被代码引用的图片：`型钢截面生成器/steel_sections/*.jpg`

> `模块/日志`、`模块/输出` 由插件在运行时用 `os.makedirs(exist_ok=True)` 自建，
> 无需随包；日志与清单都是本机运行产物，不应分发。

## 4. 一键打包（Windows PowerShell）

在**仓库根目录**执行（把 `$VERSION` 改成当前版本）：

```powershell
$VERSION = "V1.0.1"
$dirs    = @("bentley_ui", "型钢截面生成器", "管道信息查询", "管道支吊架")

$stage = Join-Path $env:TEMP "dist"
$pkg   = Join-Path $stage "管道支吊架_分发版本_$VERSION"
Remove-Item $stage -Recurse -Force -ErrorAction SilentlyContinue
New-Item -ItemType Directory -Force -Path $pkg | Out-Null

$exclude = @(
  '/E',
  '/XD', '__pycache__', '.ruff_cache', 'tests', '日志', '输出',
  '/XF', '*.pyc', '*_debug_log.txt', 'debug_log.txt', '*.log', '*.bak', '*.xlsx', '*_bom.json', '~$*', '*.jpg',
  '/NFL', '/NDL', '/NJH', '/NJS', '/NP'
)
foreach ($d in $dirs) {
  robocopy $d (Join-Path $pkg $d) @exclude | Out-Null
}

$zip = Join-Path (Get-Location) "管道支吊架_分发版本_$VERSION.zip"
Remove-Item $zip -Force -ErrorAction SilentlyContinue
Compress-Archive -Path $pkg -DestinationPath $zip -CompressionLevel Optimal
"已生成：$zip"
```

> `robocopy` 退出码 `0~7` 均表示成功（`1` = 有文件复制），**不是错误**；
> 只有 `>= 8` 才是失败。

## 5. 打包后自检

```powershell
Add-Type -AssemblyName System.IO.Compression.FileSystem
$z = [System.IO.Compression.ZipFile]::OpenRead("管道支吊架_分发版本_V1.0.1.zip")
$z.Entries.Count                              # 条目数
$z.Entries | ForEach-Object { $_.FullName }   # 逐条核对
$z.Dispose()
```

核对清单：

- [ ] 顶层四个目录齐全（`bentley_ui` / `型钢截面生成器` / `管道信息查询` / `管道支吊架`）
- [ ] `管道支吊架/` 顶层含全部入口 `*.py` 与 `README.md`
- [ ] `管道支吊架/模块/` 含全部功能子目录，**不含** `tests` / `日志` / `输出`
- [ ] `管道支吊架/模块/公共/` 含 `支吊架公共库.py`、`混凝土锚板.py`、`端焊三角架_基础.py`
- [ ] `型钢截面生成器/steel_sections/` 含全部 `steel_*.py`
- [ ] 无 `__pycache__` / `*.pyc` / `*_debug_log.txt` / `*.log` / `*_bom.json`

## 6. 新增功能后如何更新

1. 新支吊架放在 `管道支吊架/`（入口脚本）与 `管道支吊架/模块/<功能>/`
   （`_数据.py` / `_几何.py` / `.commands.xml`），并在 `管道支吊架/README.md` 登记。
2. 若**引入了新的同级依赖目录**（新的共享库），把它加进第 4 节脚本的 `$dirs`，
   并同步更新第 2 节的表格。
3. 重新执行第 4 节脚本，版本号按第 1 节规则 +1。
4. 新依赖目录同样受第 3 节排除规则约束。
