# Bentley_OPM_Plugin_wlp

为 **Bentley OpenPlant Modeler（OPM）** 开发的 Python 插件集合。所有脚本运行在 Bentley Power Platform Python 环境（`MSPyBentley` / `MSPyBentleyGeom` / `MSPyDgnPlatform` / `MSPyMstnPlatform`）中，直接在三维 DGN 模型内创建 MicroStation SmartSolid 实体。

> 这些插件绝大多数是**几何建模**工具，生成的是活动 OPM 模型中的 SmartSolid 图元（`管道信息查询` 是例外，它是只读查询工具，不生成几何）。若需注册为带 Class / Tag / 规格属性的 OpenPlant 智能组件，还需结合项目使用的 OPM 类库、EC Schema 和目录规范。

## 目录结构

| 目录 | 说明 |
| --- | --- |
| `安保围栏` | 参数化安全围栏建模，按约束生成围栏实体钢管、围栏片、立柱和螺栓 |
| `安保围栏-门` | 安保围栏大门放置工具：单扇 / 双扇门型、45° 斜接门框、菱形网、防攀悬臂与门柱基础 |
| `普通钢结构围栏` | 沿智能线创建普通钢结构围栏：立柱 + 顶/中横杆 + 连接球 + 踢脚板，四种立柱底部连接形式、三种端部闭合方式，含 ItemType 钢材清单与 JSON 导出 |
| `可拆卸栏杆` | 可拆卸钢结构栏杆：固定 DN50 套管 + 可插拔 DN40 立杆 + 定位环 + 横杆 + 130 高挡板，分离永久套管与可拆面板两个单元，含 ItemType 钢材清单与 JSON 导出 |
| `型钢截面生成器` | 统一型钢截面 / 扫掠插件：七类型钢（平行腿槽钢、普通工字钢、热轧 H 型钢、斜腿槽钢、等边/不等边角钢、HK 系列）共 1100+ 规格，先选型式再选规格，支持放置二维截面或沿路径扫掠为 SmartSolid |
| `管道支吊架` | 管道支吊架插件集 + 共享清单：集中放置端焊三角架（含选线 / 端板版）、L 型管架、门型架（角钢和槽钢）、门型架（H型钢）（均为图 C.4-8 类型 1）、T 形架（单立柱 + 顶部居中横担 · 类型 1）、保温管夹（高温隔热限位管托 · 图集 T4 · DN80~600）、G2 混凝土锚板 + 膨胀锚栓，共用统一 ItemType 契约（库 `PipeSupportComponents`）；另有只读统计插件，按类型统计套数、导出 Excel（支吊架表 + 材料汇总表）与 JSON |
| `管道信息查询` | **只读查询插件**（不生成几何）：点取一个管道 / 管道元件，读出管线号、公称直径、外径、壁厚、保温厚度、管道等级、材质等 OpenPlant EC 属性，以及由曲线算出的中心线标高、管段长度、起终点与走向，并推算保温后外径、由外径反查公称直径；属性长度单位按「属性长度 ↔ 几何长度」自动标定并支持面板手动覆盖；可导出全部 EC 属性、含纯逻辑单测 |
| `实体管口` | CL150 法兰等级管口生成器：空心钢管 + 法兰 + 密封面凸台 + 可选螺栓孔（首版） |
| `方形人孔` | 带盖板的方形人孔：筒体、盖板、U 型把手，按几何规则装配 |
| `水箱爬梯` | 水箱爬梯建模库（`ladder_lib.py`），提供爬梯 / 踏步 / 护笼等建模基础能力 |
| `墙面爬梯` | 沿所选竖直线生成墙面爬梯：立柱扁钢按支架跨距选型、φ24 横担、75×10 角钢中间支架、可选 U 形扁钢扫掠护笼（环 + 11 竖杆），含几何自检 |
| `检修爬梯` | 移动式检修爬梯（登高平台梯）：45° 斜梯 + 1 m×1 m 顶部平台（默认高 2000）+ 平台护栏 + 斜梯扶手 + 矩形底架 + 4 个万向脚轮，点取底架中心放置，含几何自检 |
| `灭火器箱` | 灭火器箱体生成工具：生成箱体、门与隔板，并通过 ItemType 构件并统计生成 JSON 清单 |
| `轻便消防水龙柜` | 按图集 15S202 第 53 页创建轻便消防水龙柜（1200×550×160）：直角柜体、隔板、卷盘、DN25 给水管（接出柜体）、阀门、喷枪与柜门，灭火器型号/数量由用户输入；通过 ItemType 记录《主要器材表》8 项器材与灭火器并导出 JSON / Excel 清单 |
| `竖直弯头耳轴` | 参数化竖直弯头耳轴：A/B/C 型底板、空心/实心耳轴、鞍口布尔运算、通气孔 |
| `罐壁人孔` | 罐壁（筒体）人孔：筒节 + 法兰 + 盖板 + 螺栓 + 把手，可选铰链或 R220 吊杆（图 BN-DS-A 4） |

## 环境要求

- Bentley OpenPlant Modeler（基于 MicroStation）
- Power Platform Python 环境（MSPy 模块）
- 三维、公制 DGN 模型

## 通用使用方法

1. 在 OPM 中打开或新建一个**三维**、公制的 DGN 模型。
2. 打开 **Utilities（实用工具）→ Python** 的 Python Editor。
3. 新建 Python Project，选择或复制对应目录中的脚本，然后执行。
4. 在弹出窗口中设置参数，确认后在模型内点取放置点；右键可取消。
5. 每个脚本的**具体参数与几何规则**见各自目录下的 `README.md`。

> 注意：脚本按当前 DGN 的 `MASTER_UNITS_PER_METRE` 换算单位，因此输入尺寸统一按 mm 解释，不要求把主单位设成 mm。

## 各模块单独说明

- `安保围栏`：见 `安保围栏/README.md`（立柱布局、GS-M16 尺寸对应、可选混凝土基础）。
- `安保围栏-门`：见 `安保围栏-门/README.md`（单扇 / 双扇门型、门柱与门扇尺寸、GS-M16 对应关系、几何自检）。
- `可拆卸栏杆`：见 `可拆卸栏杆/README.md`（DN50 套管 / DN40 立杆可插拔做法、定位环、1800 分格、130 高挡板、ItemType 钢材清单与几何自检）。
- `型钢截面生成器`：见 `型钢截面生成器/README.md`（统一入口、七类型钢截面数据与几何、放置截面 / 沿路径扫掠两种方式、`PYSTEEL` 命令、注册表扩展接口）。
- `管道支吊架`：见 `管道支吊架/README.md`（集中：端焊三角架选线+端板版 / 选线版 / 点取版、L 型管架、门型架（角钢和槽钢）（图 C.4-8 类型 1，点选竖直线＝整组中心线 + 输入净距 B，两立柱轴对称、横担以中心线为中点横跨两立柱（两端各超立柱外缘 15）、长按 L = B + 2×立柱宽度 + 30 自动计算，角钢与槽钢的立柱都与横担背靠背、立柱非通长且顶端留 10 焊接间隙）、门型架（H型钢）（同为图 C.4-8 类型 1，点选竖直线＝整组中心线 + 输入横担全长 L，两立柱轴对称、横担以中心线为中点，两端各超立柱外缘 50、净距 B = L − 100 − 2×立柱截面高，立柱与横担腹板共面、柱顶顶焊横担下翼缘）、T 形架（类型 1 正 T 形架 / 类型 2 立柱在上的吊架，点选一条竖直线＝立柱轴线（±5° 内）、H＝线长；UI 选类型 + 输入横担全长 L，横担世界水平且垂直于立柱、以该线为中点（类型 1 横担在上端、类型 2 横担在下端且管位朝上）；子项见表 3 的 A~G（∠50×6 ~ H250×250×9×14），角钢背靠背 / H 型钢端面焊接，各子项有最大允许 H / L）、保温管夹（高温隔热限位管托 · 图集 T4 · DN80~600，点选管道轴线取方向 + 指定点＝管托 L 中心；管夹夹持保温层外径、对开 45° 法兰 + 4 颗 M 螺栓，鞍座/立板/底板按表 1 的 T1/T2/T3；B/H/L 由面板输入，H＝管道（不含保温）底部到管托底面、L＝管托沿管轴总长；管道本体可选、限位块/止推件不建；编号 T4-名称-管径-温度代码-H-L-材料代码-F）、G2 混凝土锚板 + 膨胀锚栓、公共支吊架库 `支吊架公共库.py`；统一 ItemType 契约 + 聚合导出；`00-[支吊架统计].py` 只读统计并导出 Excel「支吊架表 + 材料汇总表」与 JSON；`PYLPIPERACK` / `PYPORTALFRAME` / `PYPORTALFRAMEH` / `PYTFRAME` / `PYCLAMP` 命令；N 系列设备上生根管架已并入本目录（`N8-[设备预焊件连接板]` / `N3-[设备上生根单三角架]` / `N4-[设备上生根双三角架]`，命令 `PYNPLATE` / `PYNSINGLE` / `PYNDOUBLE`）；纯逻辑单测）。
- `管道信息查询`：见 `管道信息查询/README.md`（点取管道读 OpenPlant EC 属性 + 轴线几何；`PYPIPEINFO PICK` / `PYPIPEINFO REPORT` 命令；属性长度单位自动标定与手动覆盖；只读、不生成几何）。
- `墙面爬梯`：见 `墙面爬梯/README.md`（竖直线＝墙面 + 中心投影线、立柱中心距墙 220、75×12/16/20 扁钢按支架跨距选型、φ24 横担外伸 10、75×10 角钢中间支架两端不设、可选 50×5 扁钢扫掠 U 形护笼 + 11 根 30×5 竖杆、几何自检）。
- `检修爬梯`：见 `检修爬梯/README.md`（点取底架中心放置、45° 斜梯、1 m×1 m 平台默认高 2000、后/左/右三面平台护栏 + 两侧斜梯扶手、矩形底架 + 4 只 φ125 万向脚轮（可开关）、可选后侧 X 撑与侧撑、几何自检）。
- `实体管口`：见 `实体管口/README.md`（法兰尺寸数据与建模逻辑分离，位于 `flange_data.json`）。
- `方形人孔`：见 `方形人孔/README.md`。
- `轻便消防水龙柜`：见 `轻便消防水龙柜/README.md`（15S202 第 53 页 1200×550×160 直角柜体、P380 卷盘 / DN25 给水管接出 / 阀门 / 喷枪示意、灭火器型号与数量由用户输入并计入统计、ItemType 记录《主要器材表》与灭火器、JSON + Excel 清单导出、几何自检）。
- `竖直弯头耳轴`：见 `竖直弯头耳轴/README.md`。
- `罐壁人孔`：见 `罐壁人孔/README.md`（吊杆直径表、图 BN-DS-A 4 尺寸对应关系、铰链 / 吊杆两种连接形式、几何自检）。

## 排错

部分脚本在 Bentley 几何内核返回错误时会向各自目录写入 `*_debug_log.txt`，排查时请查看对应日志。

## License

仅用于项目内部，版权归作者所有。
